# Project Rules

- Before making any changes, run `git fetch` and ensure the local branch is up to date with the remote. If behind and the working tree is clean, run `git pull`. If there are uncommitted local changes, alert the user before pulling.
